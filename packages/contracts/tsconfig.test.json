{
  "//": "Specs are excluded from the build so they never reach dist, but they must still be typechecked. This config restores them for `npm run typecheck` only.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": []
}
